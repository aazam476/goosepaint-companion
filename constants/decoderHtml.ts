// Auto-generated from public/decoder.html — do NOT hand-edit.
// Run `npm run sync-decoder` after changing public/decoder.html to regenerate.
export const DECODER_HTML = "<!DOCTYPE html><html class=\"dark\" lang=\"en\" style=\"\"><head>\n<meta charset=\"utf-8\">\n<meta content=\"width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no, viewport-fit=cover\" name=\"viewport\">\n<title>Goose Paint Decoder &amp; Solana Submitter</title>\n<!-- Tailwind CSS CDN -->\n<script src=\"https://cdn.tailwindcss.com?plugins=forms,container-queries\"></script>\n<!-- Fonts -->\n<link href=\"https://fonts.googleapis.com\" rel=\"preconnect\">\n<link crossorigin=\"\" href=\"https://fonts.gstatic.com\" rel=\"preconnect\">\n<link href=\"https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700&amp;family=JetBrains+Mono:wght@400;500;600&amp;display=swap\" rel=\"stylesheet\">\n<!-- Google Material Symbols -->\n<link href=\"https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&amp;display=swap\" rel=\"stylesheet\">\n<script>\n  // Config bootstrap: native WebView injects window.__GOOSE_PAINT_CONFIG__\n  // directly before this page runs (see constants/decoderHtml.ts +\n  // app/index.tsx). Mobile web instead loads this file as a static asset,\n  // so it can't be pre-injected — it arrives as a query param instead.\n  (function () {\n    if (window.__GOOSE_PAINT_CONFIG__) return;\n    var params = new URLSearchParams(window.location.search);\n    var apiBase = params.get(\"apiBase\") || \"\";\n    window.__GOOSE_PAINT_CONFIG__ = { apiBase: apiBase };\n  })();\n</script>\n<script>\n  tailwind.config = {\n    darkMode: 'class',\n    theme: {\n      extend: {\n        fontFamily: {\n          sans: ['\"Plus Jakarta Sans\"', 'system-ui', 'sans-serif'],\n          mono: ['\"JetBrains Mono\"', 'monospace'],\n        },\n        colors: {\n          obsidian: {\n            950: '#07080c',\n            900: '#0c0e15',\n            850: '#11141f',\n            800: '#171b29',\n            700: '#23293d',\n          },\n          solana: {\n            purple: '#9945FF',\n            teal: '#14F195',\n            cyan: '#00F0FF',\n            dark: '#181424'\n          }\n        },\n        boxShadow: {\n          glow: '0 0 20px -5px rgba(99, 102, 241, 0.35)',\n          'glow-solana': '0 0 25px -4px rgba(153, 69, 255, 0.4), 0 0 15px -2px rgba(20, 241, 149, 0.3)',\n          'inner-bevel': 'inset 0 1px 1px 0 rgba(255, 255, 255, 0.1)',\n        }\n      }\n    }\n  }\n</script>\n<style>\n  /* Base & Custom Utilities */\n  /* html needs the same dark background as body: on mobile browsers the\n     collapsing/expanding URL bar (and the safe-area inset viewport-fit=cover\n     enables above) can reveal a strip of the <html> element's own background\n     beneath body's content, which defaults to white if unset here — that's\n     the white bar some mobile browsers show at the bottom of this page. */\n  html, body {\n    background-color: #08090e;\n  }\n  body {\n    color: #f1f5f9;\n    font-family: 'Plus Jakarta Sans', system-ui, sans-serif;\n    -webkit-tap-highlight-color: transparent;\n  }\n\n  /* Canvas and video styling required by engine */\n  /* No aspect-ratio was set here before, so the video's rendered height\n     came entirely from whatever aspect ratio the camera stream reports —\n     on iOS, the rear camera (facingMode: \"environment\") often reports a\n     tall native resolution, which combined with width:100% and no height\n     cap made the viewport render much taller than intended. Capping it at\n     16:9 with object-fit: cover keeps a consistent, compact preview box\n     regardless of the actual stream's native aspect ratio. This crop DOES\n     matter for scanning: captureAndScan() replicates this same\n     object-fit: cover center-crop (see coverCropRect) before reading\n     pixels, so the corner-detection/homography pipeline only ever sees\n     the same framing the person saw and aimed the 4 corner squares\n     within — not extra field-of-view from the camera's full native frame\n     that was never visible on screen. */\n  video, canvas.snap {\n    width: 100%;\n    aspect-ratio: 16 / 9;\n    object-fit: cover;\n    border-radius: 12px;\n    background: #050608;\n    margin: 8px 0;\n  }\n\n  /* Grid styles configured for display-only badge inspection (upload/scan tab) */\n  #grid {\n    display: grid;\n    grid-template-columns: repeat(12, 1fr);\n    gap: 3px;\n    width: 100%;\n    max-width: 360px;\n    aspect-ratio: 1 / 1;\n    margin: 0 auto;\n    padding: 10px;\n    background: rgba(12, 14, 21, 0.95);\n    border-radius: 18px;\n    border: 1px solid rgba(255, 255, 255, 0.08);\n    box-shadow: inset 0 2px 10px rgba(0, 0, 0, 0.6), 0 10px 25px -5px rgba(0, 0, 0, 0.5);\n  }\n  .cell {\n    aspect-ratio: 1 / 1;\n    border-radius: 3.5px;\n    box-sizing: border-box;\n    cursor: default;\n    user-select: none;\n    transition: background-color 0.2s ease, border-color 0.2s ease;\n    border: 1px solid rgba(255, 255, 255, 0.07);\n    position: relative;\n  }\n  .badrow {\n    outline: 2px solid #f43f5e !important;\n    box-shadow: 0 0 8px rgba(244, 63, 94, 0.6);\n    z-index: 2;\n  }\n  .badcol {\n    outline: 2px solid #f59e0b !important;\n    box-shadow: 0 0 8px rgba(245, 158, 11, 0.6);\n    z-index: 2;\n  }\n  .badrow.badcol {\n    outline: 2.5px solid #ec4899 !important;\n    box-shadow: 0 0 10px rgba(236, 72, 153, 0.75);\n    z-index: 3;\n  }\n\n  /* Feed grid: same 12x12 layout, smaller/denser, no bad-checksum outline\n     styling needed since only checksum-valid images ever land on chain. */\n  .feed-grid {\n    display: grid;\n    grid-template-columns: repeat(12, 1fr);\n    gap: 1.5px;\n    background: #0b0c11;\n    padding: 3px;\n    border-radius: 8px;\n    border: 1px solid rgba(255, 255, 255, 0.06);\n  }\n  .feed-cell {\n    aspect-ratio: 1 / 1;\n    border-radius: 1.5px;\n  }\n\n  /* Custom terminal status formatting */\n  #status {\n    font-family: 'JetBrains Mono', monospace;\n    white-space: pre-wrap;\n    line-height: 1.45;\n  }\n\n  .tab-btn {\n    display: flex;\n    align-items: center;\n    justify-content: center;\n    gap: 0.5rem;\n    padding: 0.5rem 1rem;\n    font-size: 0.875rem;\n    font-weight: 500;\n    border-radius: 9999px;\n    white-space: nowrap;\n    transition: color 0.15s ease, background-color 0.15s ease, border-color 0.15s ease;\n    color: #a1a1aa;\n    border: 1px solid transparent;\n  }\n  .tab-btn:hover { color: #ffffff; }\n  .tab-btn.active {\n    background: rgba(16, 185, 129, 0.15);\n    color: #34d399;\n    border-color: rgba(16, 185, 129, 0.3);\n    box-shadow: 0 0 12px rgba(16, 185, 129, 0.2);\n  }\n</style>\n</head>\n<body class=\"min-h-screen bg-obsidian-950 text-slate-100 flex flex-col items-center pb-28 selection:bg-solana-purple selection:text-white\">\n<!-- Mobile Top Navigation Header -->\n<header class=\"w-full sticky top-0 z-40 bg-obsidian-950/85 backdrop-blur-xl border-b border-white/10 px-4 py-3\">\n<div class=\"max-w-md mx-auto flex items-center justify-between\">\n<div class=\"flex items-center gap-3\">\n<!-- Wordmark, matching the desktop handoff screen's GOOSE//PAINT branding\n     (components/DesktopHandoff.tsx) rather than a separate icon+title. -->\n<div>\n<div class=\"flex items-center gap-2\">\n<span class=\"w-2 h-2 rounded-full bg-emerald-400 flex-shrink-0\"></span>\n<h1 class=\"text-sm font-semibold tracking-[0.2em] text-slate-200 m-0 leading-none font-mono\">GOOSE//PAINT</h1>\n</div>\n<p class=\"text-xs text-slate-400 m-0 mt-1\">Badge Decoder &amp; On-Chain Broadcaster</p>\n</div>\n</div>\n</div>\n</header>\n<!-- Main Responsive Container -->\n<main class=\"w-full max-w-md px-4 pt-4 flex flex-col gap-5\">\n\n<!-- ============================================== -->\n<!-- SECTION: DOOMSCROLL FEED (\"DRAWINGS\")          -->\n<!-- ============================================== -->\n<section class=\"flex flex-col gap-4\" id=\"feedSection\">\n  <div id=\"feedList\" class=\"flex flex-col gap-4\"></div>\n  <div class=\"py-6 flex flex-col items-center justify-center text-center text-slate-500 text-xs font-mono\" id=\"feedStatus\">\n    <div class=\"w-5 h-5 border-2 border-emerald-400 border-t-transparent rounded-full animate-spin mb-2\"></div>\n    <span>Syncing on-chain badge blocks...</span>\n  </div>\n  <button class=\"mx-auto text-[11px] font-mono text-slate-400 hover:text-slate-200 bg-slate-800/70 px-3 py-1.5 rounded-full border border-slate-700 hidden\" id=\"feedLoadMoreBtn\" type=\"button\">\n    Load more\n  </button>\n</section>\n\n<!-- ============================================== -->\n<!-- SECTION: UPLOAD / SCAN                         -->\n<!-- ============================================== -->\n<section class=\"hidden flex-col gap-5\" id=\"uploadSection\">\n<!-- CARD 1: Camera & Badge Scanner -->\n<div class=\"bg-obsidian-900/90 border border-white/10 rounded-2xl p-4 shadow-xl relative overflow-hidden backdrop-blur-md\">\n<!-- Ambient Glow Top Edge -->\n<div class=\"absolute -top-12 left-1/2 -translate-x-1/2 w-48 h-24 bg-purple-500/15 blur-3xl pointer-events-none\"></div>\n<div class=\"flex items-center gap-2 mb-3\">\n<span class=\"material-symbols-outlined text-purple-400 text-lg\">photo_camera</span>\n<h2 class=\"text-sm font-semibold tracking-wide uppercase text-slate-200\">Hardware Scanner</h2>\n</div>\n<p class=\"text-xs text-slate-400 mb-3 leading-relaxed\">\n  Align the 4 corner squares of your badge screen in frame to auto-rectify and extract pixel matrix data.\n</p>\n<!-- Viewport Feed Area -->\n<div class=\"relative rounded-xl overflow-hidden bg-black border border-white/10 mb-3 min-h-[44px] flex flex-col items-center justify-center\" id=\"viewfinderWrapper\">\n<!-- Target Reticle Overlay -->\n<div class=\"absolute inset-0 pointer-events-none z-10 flex flex-col justify-between p-3 opacity-40\">\n<div class=\"flex justify-between\">\n<div class=\"w-5 h-5 border-t-2 border-l-2 border-teal-400 rounded-tl-sm\"></div>\n<div class=\"w-5 h-5 border-t-2 border-r-2 border-teal-400 rounded-tr-sm\"></div>\n</div>\n<div class=\"flex justify-between\">\n<div class=\"w-5 h-5 border-b-2 border-l-2 border-teal-400 rounded-bl-sm\"></div>\n<div class=\"w-5 h-5 border-b-2 border-r-2 border-teal-400 rounded-br-sm\"></div>\n</div>\n</div>\n<!-- Video & Canvas elements with exact IDs -->\n<video id=\"video\" muted=\"\" playsinline=\"\" style=\"display:none\"></video>\n<canvas class=\"snap\" id=\"snap\" style=\"display:none\"></canvas>\n<canvas class=\"snap\" id=\"flat\" style=\"display:none\"></canvas>\n<div class=\"py-5 text-center px-4 text-slate-500 text-xs flex flex-col items-center gap-1.5\" id=\"camPlaceholder\">\n<span class=\"material-symbols-outlined text-2xl text-slate-600\">videocam_off</span>\n<span class=\"\">Camera stream is inactive</span>\n</div>\n<!-- Shown in the \"review\" state only when corner detection failed, so\n     there's no rectified image to display and the raw captured photo is\n     intentionally not shown either (see setCamState). -->\n<div class=\"py-5 text-center px-4 text-slate-500 text-xs flex flex-col items-center gap-1.5\" id=\"noRectifiedPlaceholder\" style=\"display:none\">\n<span class=\"material-symbols-outlined text-2xl text-slate-600\">crop_free</span>\n<span class=\"\">No rectified preview available — corners weren't found, but the raw photo was still OCR'd.</span>\n</div>\n</div>\n<!-- Single scanner control: cycles Start Camera -> Capture -> Retake.\n     See camState/setCamState in the script below for the state machine;\n     capturing immediately triggers OCR with no separate \"Scan\" step. -->\n<button class=\"w-full flex items-center justify-center gap-2 py-2.5 px-3 rounded-xl bg-slate-800/90 hover:bg-slate-700/80 active:scale-[0.98] border border-white/10 text-slate-200 text-xs font-semibold transition-all shadow-sm\" id=\"camBtn\" type=\"button\">\n<span class=\"material-symbols-outlined text-sm text-purple-400\" id=\"camBtnIcon\">videocam</span>\n<span id=\"camBtnLabel\">Start Camera</span>\n</button>\n</div>\n<!-- CARD 2: Manual Code Input & Telemetry Status -->\n<div class=\"bg-obsidian-900/90 border border-white/10 rounded-2xl p-4 shadow-xl backdrop-blur-md\">\n<div class=\"flex items-center justify-between mb-2\">\n<div class=\"flex items-center gap-2\">\n<span class=\"material-symbols-outlined text-teal-400 text-lg\">data_object</span>\n<h2 class=\"text-sm font-semibold tracking-wide uppercase text-slate-200\">Raw Code Input</h2>\n</div>\n<div class=\"flex gap-1.5\">\n<button class=\"text-[11px] font-mono text-slate-400 hover:text-slate-200 bg-slate-800 px-2 py-0.5 rounded border border-slate-700 active:scale-95 transition\" onclick=\"document.getElementById('code').value=''; document.getElementById('code').focus();\" type=\"button\">Clear</button>\n</div>\n</div>\n<p class=\"text-xs text-slate-400 mb-2 leading-relaxed\">\n  Or paste the 169-character encoded string (spaces and breaks ignored):\n</p>\n<!-- Code Textarea -->\n<div class=\"relative mb-3\">\n<textarea class=\"w-full h-24 p-3 font-mono text-xs text-teal-200 placeholder-slate-600 bg-obsidian-950/90 border border-white/15 rounded-xl focus:border-teal-400 focus:ring-1 focus:ring-teal-400 outline-none resize-none transition-all shadow-inner\" id=\"code\" placeholder=\"GP1X XXXX ...\" spellcheck=\"false\"></textarea>\n</div>\n<!-- Action Button -->\n<div>\n<button class=\"w-full flex items-center justify-center gap-2 py-3 px-4 rounded-xl bg-gradient-to-r from-purple-600 via-indigo-600 to-purple-600 hover:brightness-110 active:scale-[0.98] text-white text-sm font-bold shadow-lg shadow-purple-600/30 border border-purple-400/30 transition-all\" id=\"decodeBtn\" type=\"button\">\n<span class=\"material-symbols-outlined text-base\">auto_fix_high</span>\n<span class=\"\">Decode Pattern</span>\n</button>\n</div>\n<!-- Telemetry Output Box -->\n<div class=\"mt-3 p-3 bg-obsidian-950 border border-white/5 rounded-xl overflow-x-auto\">\n<div class=\"flex items-center justify-between pb-1.5 mb-1.5 border-b border-white/5 text-[10px] font-mono text-slate-500\">\n<span class=\"flex items-center gap-1\"><span class=\"w-1.5 h-1.5 rounded-full bg-slate-500\"></span>PARITY CHECKS</span>\n<span class=\"text-slate-600\">MOD 17</span>\n</div>\n<div class=\"text-xs text-slate-300 min-h-[20px]\" id=\"status\">Scan a badge or paste a code to begin.</div>\n</div>\n</div>\n<!-- CARD 3: 12x12 Decoded Matrix & Solana Broadcast -->\n<div class=\"bg-obsidian-900/90 border border-white/10 rounded-2xl p-4 shadow-xl backdrop-blur-md relative overflow-hidden\">\n<div class=\"flex items-center justify-between mb-3\">\n<div class=\"flex items-center gap-2\">\n<span class=\"material-symbols-outlined text-teal-400 text-lg\">view_in_ar</span>\n<h2 class=\"text-sm font-semibold tracking-wide uppercase text-slate-200\">Decoded Badge Matrix</h2>\n</div>\n<div class=\"flex items-center gap-2\">\n<span class=\"text-[10px] font-mono px-2 py-0.5 rounded bg-purple-950/70 border border-purple-500/30 text-purple-300\">READ-ONLY</span>\n<span class=\"text-[11px] font-mono px-2 py-0.5 rounded bg-slate-800/80 border border-white/5 text-slate-400\">12 × 12 (144 px)</span>\n</div>\n</div>\n<p class=\"text-xs text-slate-400 mb-3\">\n  Direct badge screen telemetry representation. Verified pixels are sealed and broadcasted immutable to Solana.\n</p>\n<!-- Pixel Grid Element with exact ID (Display-Only Preview Matrix) -->\n<div class=\"flex justify-center mb-4\">\n<div id=\"grid\"></div>\n</div>\n<!-- Web3 / Solana Submission Section -->\n<div class=\"border-t border-white/10 pt-4 flex flex-col gap-3\">\n<!-- Solana Submit Action Button -->\n<button class=\"relative group overflow-hidden w-full flex items-center justify-center gap-2.5 py-3.5 px-4 rounded-xl bg-gradient-to-r from-[#9945FF] via-[#7B2BF9] to-[#14F195] hover:brightness-110 active:scale-[0.98] disabled:opacity-40 disabled:pointer-events-none text-white text-sm font-bold shadow-glow-solana transition-all\" disabled id=\"solanaSubmitBtn\" type=\"button\">\n<!-- Official Solana Vector Logo -->\n<svg class=\"w-5 h-4 flex-shrink-0 drop-shadow\" fill=\"none\" viewBox=\"0 0 397 311\" xmlns=\"http://www.w3.org/2000/svg\">\n<path clip-rule=\"evenodd\" d=\"M64.6 237.9c2.4-2.4 5.7-3.8 9.2-3.8h317.4c5.8 0 8.7 7 4.6 11.1l-62.7 62.7c-2.4 2.4-5.7 3.8-9.2 3.8H6.5c-5.8 0-8.7-7-4.6-11.1l62.7-62.7zM64.6 3.8C67 1.4 70.3 0 73.8 0h317.4c5.8 0 8.7 7 4.6 11.1l-62.7 62.7c-2.4 2.4-5.7 3.8-9.2 3.8H6.5c-5.8 0-8.7-7-4.6-11.1L64.6 3.8zm267.8 115.1c-2.4-2.4-5.7-3.8-9.2-3.8H5.8c-5.8 0-8.7 7-4.6 11.1l62.7 62.7c2.4 2.4 5.7 3.8 9.2 3.8h317.4c5.8 0 8.7-7 4.6-11.1l-62.7-62.7z\" fill=\"currentColor\" fill-rule=\"evenodd\"></path>\n</svg>\n<span id=\"submitBtnLabel\" class=\"\">Publish New Image</span>\n</button>\n<!-- Context note for Doomscroll Viewer App -->\n<div class=\"px-2 pt-0.5 text-center\">\n<p class=\"text-[11px] text-slate-400 leading-normal m-0\">\n  Decoded badge patterns are broadcast live to the Solana on-chain stream for the <span class=\"text-teal-300 font-medium cursor-pointer hover:underline\" onclick=\"switchToTab('feed')\">Drawings tab</span>.\n</p>\n</div>\n<!-- On-chain Transaction Toast / Status Box (Dynamic) -->\n<div class=\"hidden p-3 rounded-xl bg-purple-950/40 border border-purple-500/30 font-mono text-xs flex-col gap-1.5 transition-all\" id=\"solanaTxCard\">\n<div class=\"flex items-center justify-between\">\n<span class=\"text-solana-teal flex items-center gap-1 font-semibold\">\n<span class=\"material-symbols-outlined text-sm\">verified</span> Confirmed On-Chain\n</span>\n<span class=\"text-[10px] text-slate-400\" id=\"txSlot\">Slot #—</span>\n</div>\n<div class=\"flex items-center justify-between text-[11px] text-slate-300 bg-obsidian-950/80 px-2 py-1.5 rounded border border-white/5\">\n<span class=\"text-slate-500\">TX:</span>\n<span class=\"text-purple-300 truncate max-w-[210px]\" id=\"txSig\" title=\"\">—</span>\n<button class=\"text-teal-400 hover:text-teal-300 ml-1 text-[10px]\" onclick=\"copyTxSignature()\" type=\"button\">Copy</button>\n</div>\n<div class=\"text-[10px] text-slate-400 flex items-center justify-between\">\n<span class=\"\">Feed payload: 144 nibbles</span>\n<span class=\"text-emerald-400 cursor-pointer hover:underline\" onclick=\"switchToTab('feed')\">View in Drawings →</span>\n</div>\n</div>\n</div>\n</div>\n</section>\n</main>\n\n<!-- ============================================== -->\n<!-- FLOATING BOTTOM NAVIGATION BAR                 -->\n<!-- ============================================== -->\n<div class=\"fixed bottom-6 left-0 right-0 z-50 flex justify-center items-center pointer-events-none px-4\">\n<nav class=\"pointer-events-auto flex items-center bg-[#15161c]/90 backdrop-blur-xl border border-white/10 p-1.5 rounded-full shadow-2xl shadow-black/80 gap-1.5 min-w-[320px] max-w-md w-full justify-between\">\n<button class=\"tab-btn flex-1 active\" id=\"tabFeedBtn\" type=\"button\" onclick=\"switchToTab('feed')\">\n<span class=\"w-1.5 h-1.5 rounded-full bg-emerald-400 animate-pulse flex-shrink-0\"></span>\n<span class=\"material-symbols-outlined text-base\">grid_view</span>\n<span class=\"whitespace-nowrap\">Drawings</span>\n</button>\n<button class=\"tab-btn flex-1\" id=\"tabUploadBtn\" type=\"button\" onclick=\"switchToTab('upload')\">\n<span class=\"material-symbols-outlined text-base\">document_scanner</span>\n<span class=\"whitespace-nowrap\">Upload / Scan</span>\n</button>\n</nav>\n</div>\n\n<!-- EXACT Tesseract Script Tag Required -->\n<script src=\"https://cdn.jsdelivr.net/npm/tesseract.js@5/dist/tesseract.min.js\"></script>\n<script>\nconst ALPHA = \"XAEFHKLMNPRTYJ479\";\n// Canonical 17-color badge palette (index 0 = white/blank). This is the\n// only palette the encoding is defined against — do not swap in a\n// different palette without re-deriving how badges map colors to codes,\n// since every already-programmed badge assumes this exact table.\nconst COLORS = [0xffffff,\n  0x000000, 0xe53935, 0xfb8c00, 0xfdd835,\n  0x43a047, 0x26a69a, 0x00acc1, 0x1e88e5,\n  0x5e35b1, 0xd81b60, 0x8d6e63, 0x9e9e9e,\n  0xffab91, 0xa5d6a7, 0x90caf9, 0xce93d8];\nconst N = 12;\nconst FW = 640, FH = 480;         // flattened screen size (2x the 320x240 badge)\nlet pix = new Array(N * N).fill(0);\nlet badRows = new Set(), badCols = new Set();\nlet lastDecodedPayload = \"\";\nlet lastDecodeOk = false;\nconst hex = c => \"#\" + c.toString(16).padStart(6, \"0\");\nconst $ = id => document.getElementById(id);\n\nfunction apiBase() {\n  return (window.__GOOSE_PAINT_CONFIG__ && window.__GOOSE_PAINT_CONFIG__.apiBase) || \"\";\n}\n\n// ---- decoding the text ----\n\n// Runs the mod-17 row/column/total checksum validation against a 169-char\n// value array (v[i] = ALPHA index of character i, or undefined if that\n// position couldn't be mapped to a known letter) and returns which rows,\n// columns, and the total check failed — without mutating any shared\n// state. Factored out of decode() so the auto-correction pass below can\n// try a trial substitution and re-check it cheaply, without duplicating\n// this logic or touching the real badRows/badCols/pix state until a\n// correction is actually accepted.\nfunction validate(v) {\n  const badRows = new Set(), badCols = new Set();\n  const cell = new Array(N * N).fill(0);\n  for (let y = 0; y < N; y++) {\n    let sum = 0;\n    for (let x = 0; x < N; x++) {\n      const val = v[y * 13 + x] ?? 0;\n      cell[y * N + x] = val;\n      sum += val;\n    }\n    const chk = v[y * 13 + 12];\n    if (chk === undefined || sum % 17 !== chk) badRows.add(y);\n  }\n  let total = 0;\n  for (let x = 0; x < N; x++) {\n    let sum = 0;\n    for (let y = 0; y < N; y++) sum += cell[y * N + x];\n    total += sum;\n    const chk = v[156 + x];\n    if (chk === undefined || sum % 17 !== chk) badCols.add(x);\n  }\n  const totalOk = v[168] !== undefined && total % 17 === v[168];\n  return { badRows, badCols, totalOk, cell };\n}\n\n// Attempts to fix single-character OCR mistakes using the checksums\n// themselves as ground truth, rather than only reporting that something's\n// wrong. Every one of the 144 data cells participates in exactly one row\n// checksum, one column checksum, and the total checksum; a row or column's\n// own checksum character participates only in that one check. That means\n// a single wrong character breaks a small, specific, predictable set of\n// checks — which is exactly the structure needed to search for \"what one\n// change would make this check pass without breaking anything else.\"\n//\n// For each failing row, this tries every (position in that row) x (every\n// alphabet letter) substitution and keeps only ones that fix the row\n// without breaking a column or the total that was previously fine. If\n// exactly one such fix exists for a row, it's applied — high confidence,\n// since a coincidental second unrelated error would be needed to produce\n// a false positive. If none or multiple candidate fixes exist, that row\n// is left alone rather than guessed at; ambiguous corrections are worse\n// than an honest \"please re-scan.\" The same then runs for failing\n// columns using whatever the row pass already fixed.\nfunction autoCorrect(v) {\n  const original = validate(v);\n  let corrected = v.slice();\n  let anyFix = false;\n\n  function tryFixRow(y) {\n    const candidates = [];\n    for (let pos = y * 13; pos < y * 13 + 13; pos++) {\n      const isRowChecksumPos = (pos % 13 === 12);\n      // A bad column can only be caused by a bad DATA cell -- column sums\n      // never read a row's own checksum character. So if any column is\n      // already flagged bad, this row's checksum char can't be the real\n      // culprit: including it here just adds a spurious second \"fix\" that\n      // makes the row look consistent with already-corrupted data,\n      // turning an unambiguous single-candidate fix into a false tie.\n      if (isRowChecksumPos && original.badCols.size > 0) continue;\n      for (let letter = 0; letter < ALPHA.length; letter++) {\n        if (corrected[pos] === letter) continue;\n        const trial = corrected.slice();\n        trial[pos] = letter;\n        const check = validate(trial);\n        if (check.badRows.has(y)) continue; // didn't even fix this row\n        // Don't accept a \"fix\" that breaks something else that was fine.\n        const brokeAnotherRow = [...check.badRows].some((r) => r !== y && !original.badRows.has(r));\n        const brokeAColumn = [...check.badCols].some((c) => !original.badCols.has(c));\n        const brokeTotal = original.totalOk && !check.totalOk;\n        if (brokeAnotherRow || brokeAColumn || brokeTotal) continue;\n        candidates.push({ pos, letter });\n      }\n    }\n    if (candidates.length === 1) {\n      corrected[candidates[0].pos] = candidates[0].letter;\n      anyFix = true;\n    }\n  }\n\n  function tryFixCol(x) {\n    const positions = [];\n    for (let y = 0; y < N; y++) positions.push(y * 13 + x); // 12 data cells\n    // Symmetric to tryFixRow: a bad row can only be caused by a bad DATA\n    // cell, so if any row is already flagged bad, this column's own\n    // checksum char (156+x) can't be the real culprit -- only consider it\n    // when no row is flagged, otherwise it creates the same false tie.\n    if (original.badRows.size === 0) positions.push(156 + x);\n    const candidates = [];\n    for (const pos of positions) {\n      for (let letter = 0; letter < ALPHA.length; letter++) {\n        if (corrected[pos] === letter) continue;\n        const trial = corrected.slice();\n        trial[pos] = letter;\n        const check = validate(trial);\n        if (check.badCols.has(x)) continue;\n        const brokeAnotherCol = [...check.badCols].some((c) => c !== x && !original.badCols.has(c));\n        const brokeARow = [...check.badRows].some((r) => !original.badRows.has(r));\n        const brokeTotal = original.totalOk && !check.totalOk;\n        if (brokeAnotherCol || brokeARow || brokeTotal) continue;\n        candidates.push({ pos, letter });\n      }\n    }\n    if (candidates.length === 1) {\n      corrected[candidates[0].pos] = candidates[0].letter;\n      anyFix = true;\n    }\n  }\n\n  for (const y of original.badRows) tryFixRow(y);\n  for (const x of original.badCols) tryFixCol(x);\n\n  // A single corrupted TOTAL-checksum character shows up as every row and\n  // column passing, but the grand total mismatching -- the only possible\n  // culprit is v[168] itself, so just search for the letter that makes it\n  // agree with the (already-trusted) row/column checksums.\n  if (original.badRows.size === 0 && original.badCols.size === 0 && !original.totalOk) {\n    for (let letter = 0; letter < ALPHA.length; letter++) {\n      if (corrected[168] === letter) continue;\n      const trial = corrected.slice();\n      trial[168] = letter;\n      if (validate(trial).totalOk) {\n        corrected[168] = letter;\n        anyFix = true;\n        break;\n      }\n    }\n  }\n\n  return { corrected, anyFix };\n}\n\nfunction decode(text, opts) {\n  opts = opts || {};\n  const msgs = [];\n  // Keep only alphabet characters. The tag \"GP1\" leaves a single \"P\".\n  let s = text.toUpperCase().replace(/[^XAEFHKLMNPRTYJ479]/g, \"\");\n  if (s.length === 170 && s[0] === \"P\") s = s.slice(1);   // tag remnant\n  if (s.length > 169) s = s.slice(s.length - 169);        // keep the tail\n  if (s.length !== 169)\n    msgs.push(\"Expected 169 data characters, got \" + s.length +\n              \". Fix the text or re-scan.\");\n  let v = s.split(\"\").map(ch => ALPHA.indexOf(ch)).map((i) => (i < 0 ? undefined : i));\n\n  let didAutoCorrect = false;\n  if (opts.glyphScores && s.length === 169 && v.every((x) => x !== undefined)) {\n    // Scanned by the glyph matcher: repairs are chosen from the symbols it\n    // actually ranked highly, so a fix has to look right as well as add up.\n    const before = validate(v);\n    if (before.badRows.size || before.badCols.size || !before.totalOk) {\n      const res = FIX.correct(v, opts.glyphScores, ALPHA);\n      if (res && res.v && res.v.some((x, i) => x !== v[i])) {\n        v = res.v;\n        s = v.map((i) => ALPHA[i] ?? \"?\").join(\"\");\n        didAutoCorrect = true;\n      }\n    }\n  } else if (opts.autoCorrect && s.length === 169) {\n    const initial = validate(v);\n    if (initial.badRows.size || initial.badCols.size) {\n      const { corrected, anyFix } = autoCorrect(v);\n      if (anyFix) {\n        v = corrected;\n        s = v.map((i) => ALPHA[i] ?? \"?\").join(\"\");\n        didAutoCorrect = true;\n      }\n    }\n  }\n\n  const { badRows: newBadRows, badCols: newBadCols, totalOk, cell } = validate(v);\n  badRows = newBadRows; badCols = newBadCols;\n  pix = cell;\n  lastDecodedPayload = s;\n\n  const submitBtn = $(\"solanaSubmitBtn\");\n\n  if (didAutoCorrect) msgs.push(\"Auto-corrected \" + \"a likely OCR misread using the checksums.\");\n  if (badRows.size) msgs.push(\"Rows to check: \" + [...badRows].map(r => r + 1).join(\", \"));\n  if (badCols.size) msgs.push(\"Columns to check: \" + [...badCols].map(c => c + 1).join(\", \"));\n\n  lastDecodeOk = s.length === 169 && !badRows.size && !badCols.size && totalOk;\n\n  if (lastDecodeOk) {\n    msgs.push(\"All checks passed. Matrix verified ready for Solana broadcast.\");\n  } else if (!totalOk) {\n    msgs.push(\"Total check does not match.\");\n  }\n  // Only a checksum-clean matrix is allowed on-chain: the gas station\n  // re-validates server-side too, but gating the button here avoids\n  // burning a free submission slot on a scan the server will reject.\n  if (submitBtn) submitBtn.disabled = !lastDecodeOk;\n  $(\"status\").textContent = msgs.join(\"\\n\");\n  render();\n}\n\n// Display-Only preview matrix renderer (no painting/interaction click handlers)\nfunction render() {\n  const g = $(\"grid\");\n  g.innerHTML = \"\";\n  pix.forEach((val, i) => {\n    const d = document.createElement(\"div\");\n    const x = i % N, y = Math.floor(i / N);\n    d.className = \"cell\" + (badRows.has(y) ? \" badrow\" : \"\") + (badCols.has(x) ? \" badcol\" : \"\");\n    d.style.background = hex(COLORS[val] ?? 0xffffff);\n    // Display-only inspection matrix: no manual click-to-paint attached\n    g.appendChild(d);\n  });\n}\n\n$(\"decodeBtn\").onclick = () => decode($(\"code\").value);\n\n// ---- finding the four corner squares and flattening the screen ----\n//\n// The badge firmware (see the Lua source) draws four solid white 20x20\n// squares at the four corners of its 320x240 screen, specifically so this\n// step can find them and undo whatever rotation/perspective the camera\n// introduced. The original approach here searched a fixed quadrant of the\n// *entire captured frame* for the brightest pixels and assumed that was\n// \"the\" corner square for that quadrant — which only works when the badge\n// fills nearly the whole frame and is held close to upright. The moment\n// the badge is smaller, off-center, or at a real angle (the normal case\n// for a handheld phone shot), a frame-quadrant boundary has no\n// relationship to where the badge's actual corners are, and this could\n// silently pick up the wrong bright region (part of a different corner\n// square, a reflection, background light) without ever failing outright,\n// producing a badly warped \"flattened\" image like the one that prompted\n// this rewrite.\n//\n// This replaces that with real connected-component (blob) detection\n// across the WHOLE frame: find every distinct bright blob, keep the ones\n// shaped and sized like a corner marker, then take the best 4 and\n// classify them into top-left/top-right/bottom-left/bottom-right by their\n// position relative to each other — not by which quadrant of the frame\n// they happen to land in. This works regardless of where in the frame the\n// badge actually is, how large it is, or how it's rotated.\nfunction findCornerBlobs(img) {\n  const w = img.width, h = img.height, d = img.data;\n\n  // Sample brightness at every pixel (no downscale here — accuracy of the\n  // blob's true extent matters more than raw speed at this stage, and a\n  // typical phone photo is small enough that this is still fast).\n  const gray = new Uint8ClampedArray(w * h);\n  for (let i = 0, p = 0; i < d.length; i += 4, p++) {\n    gray[p] = (d[i] + d[i + 1] + d[i + 2]) / 3;\n  }\n\n  // A global percentile threshold (rather than a fixed brightness value)\n  // adapts to the actual photo's lighting/exposure, same intent as the\n  // original per-quadrant threshold, just computed once over the whole\n  // frame instead of once per assumed-quadrant.\n  const sorted = Uint8Array.from(gray).sort();\n  const thr = sorted[Math.floor(sorted.length * 0.97)] || 255;\n\n  // Flood-fill (iterative, explicit stack — recursion would blow the\n  // stack on a large bright region like an overexposed sky) every bright\n  // pixel into its connected blob, 4-connected is enough for compact\n  // squares and keeps this cheap.\n  const visited = new Uint8Array(w * h);\n  const blobs = [];\n  const stackX = new Int32Array(w * h);\n  const stackY = new Int32Array(w * h);\n\n  for (let y = 0; y < h; y++) {\n    for (let x = 0; x < w; x++) {\n      const idx = y * w + x;\n      if (visited[idx] || gray[idx] < thr) continue;\n\n      let sp = 0;\n      stackX[sp] = x; stackY[sp] = y; sp++;\n      visited[idx] = 1;\n\n      let minX = x, maxX = x, minY = y, maxY = y, count = 0, sx = 0, sy = 0;\n\n      while (sp > 0) {\n        sp--;\n        const cx = stackX[sp], cy = stackY[sp];\n        count++; sx += cx; sy += cy;\n        if (cx < minX) minX = cx;\n        if (cx > maxX) maxX = cx;\n        if (cy < minY) minY = cy;\n        if (cy > maxY) maxY = cy;\n\n        const neighbors = [\n          [cx + 1, cy], [cx - 1, cy], [cx, cy + 1], [cx, cy - 1],\n        ];\n        for (const [nx, ny] of neighbors) {\n          if (nx < 0 || nx >= w || ny < 0 || ny >= h) continue;\n          const nidx = ny * w + nx;\n          if (visited[nidx] || gray[nidx] < thr) continue;\n          visited[nidx] = 1;\n          stackX[sp] = nx; stackY[sp] = ny; sp++;\n        }\n      }\n\n      // Filter to blobs shaped/sized like one of the badge's 20x20 corner\n      // squares. Real-world scale varies with camera distance, so this is\n      // deliberately generous on size (a wide but plausible pixel-area\n      // range) while still being strict on shape: a genuine square marker\n      // has near-equal width and height and is reasonably filled-in\n      // (count close to its bounding box area) — this rejects long thin\n      // reflections/glare streaks and large overexposed regions that\n      // would otherwise pass a size check alone.\n      const bw = maxX - minX + 1, bh = maxY - minY + 1;\n      const boxArea = bw * bh;\n      const fillRatio = count / boxArea;\n      const aspect = bw / bh;\n      const plausibleSize = boxArea >= 20 && boxArea <= (w * h) / 8;\n      const plausibleShape = fillRatio > 0.55 && aspect > 0.5 && aspect < 2;\n\n      if (plausibleSize && plausibleShape) {\n        blobs.push({ x: sx / count, y: sy / count, area: boxArea });\n      }\n    }\n  }\n\n  return blobs;\n}\n\n// Given candidate blobs (usually more than 4 — background clutter can\n// produce plausible-looking blobs too), pick the best 4 and classify them\n// as tl/tr/bl/br by position. Corner-marker squares are typically among\n// the largest clean blobs in frame, so preferring larger candidates when\n// there are more than 4 is a reasonable heuristic; classifying strictly\n// by position (leftmost-two vs rightmost-two, then top vs bottom within\n// each pair) works regardless of the badge's rotation within the frame,\n// as long as that rotation is mild (a real photo, not a photo taken\n// sideways) — which matches what the on-screen scanning guidance asks for.\nfunction classifyCorners(blobs) {\n  if (blobs.length < 4) return null;\n\n  const top4 = [...blobs].sort((a, b) => b.area - a.area).slice(0, 4);\n  const byX = [...top4].sort((a, b) => a.x - b.x);\n  const left2 = byX.slice(0, 2).sort((a, b) => a.y - b.y);\n  const right2 = byX.slice(2, 4).sort((a, b) => a.y - b.y);\n\n  const tl = left2[0], bl = left2[1];\n  const tr = right2[0], br = right2[1];\n\n  // Sanity-check the resulting quad before trusting it. If the badge isn't\n  // fully in frame (a corner square cropped out or hidden), or a big glare\n  // streak got flood-filled into a blob large enough to out-rank a real\n  // corner, \"top 4 by area\" can end up with fewer than 4 genuine corner\n  // squares. There's no way to detect that from blob shape alone once it's\n  // already past the per-blob filter in findCornerBlobs, but a quad built\n  // from real corners has known structure: roughly parallel top/bottom\n  // edges, a sane aspect ratio, and actual left-to-right / top-to-bottom\n  // separation. A glare-derived \"corner\" tends to break at least one of\n  // these badly. Rejecting here and returning null falls back to\n  // whole-image OCR (see captureAndScan) -- a blurrier but honest read\n  // beats a confidently wrong perspective warp.\n  if (!isPlausibleQuad(tl, tr, bl, br)) return null;\n\n  return {\n    tl: [tl.x, tl.y], tr: [tr.x, tr.y],\n    bl: [bl.x, bl.y], br: [br.x, br.y],\n  };\n}\n\nfunction isPlausibleQuad(tl, tr, bl, br) {\n  const dist = (a, b) => Math.hypot(a.x - b.x, a.y - b.y);\n  const topW = dist(tl, tr), botW = dist(bl, br);\n  const leftH = dist(tl, bl), rightH = dist(tr, br);\n\n  // Degenerate: any two \"corners\" landed on (near) the same point, which\n  // happens when there weren't actually 4 distinct markers to find.\n  const minSpan = 10;\n  if (topW < minSpan || botW < minSpan || leftH < minSpan || rightH < minSpan) return false;\n\n  // Opposite edges of a real (even steeply angled) badge photo shouldn't\n  // differ wildly in length -- a glare blob standing in for one corner\n  // usually collapses or stretches one edge relative to its opposite.\n  const edgeRatioOk = (a, b) => {\n    const r = a / b;\n    return r > 0.35 && r < 2.85;\n  };\n  if (!edgeRatioOk(topW, botW) || !edgeRatioOk(leftH, rightH)) return false;\n\n  // The overall shape should be roughly landscape-ish, matching the\n  // badge's real 320x240 aspect -- not a thin diagonal sliver, which is\n  // what a corner-shaped glare streak typically produces.\n  const avgW = (topW + botW) / 2, avgH = (leftH + rightH) / 2;\n  const aspect = avgW / avgH;\n  if (aspect < 0.5 || aspect > 4.5) return false;\n\n  // The quad should be (approximately) convex / non-self-intersecting: tl\n  // and tr should both sit above bl and br, not interleaved with them.\n  const avgTopY = (tl.y + tr.y) / 2, avgBotY = (bl.y + br.y) / 2;\n  if (avgTopY >= avgBotY) return false;\n\n  return true;\n}\n\nfunction homography(src, dst) {\n  const A = [], b = [];\n  for (let i = 0; i < 4; i++) {\n    const [x, y] = src[i], [u, v] = dst[i];\n    A.push([x, y, 1, 0, 0, 0, -u * x, -u * y]); b.push(u);\n    A.push([0, 0, 0, x, y, 1, -v * x, -v * y]); b.push(v);\n  }\n  for (let c = 0; c < 8; c++) {\n    let p = c;\n    for (let r = c + 1; r < 8; r++) if (Math.abs(A[r][c]) > Math.abs(A[p][c])) p = r;\n    [A[c], A[p]] = [A[p], A[c]]; [b[c], b[p]] = [b[p], b[c]];\n    for (let r = c + 1; r < 8; r++) {\n      const f = A[r][c] / A[c][c];\n      for (let k = c; k < 8; k++) A[r][k] -= f * A[c][k];\n      b[r] -= f * b[c];\n    }\n  }\n  const h = new Array(8);\n  for (let r = 7; r >= 0; r--) {\n    let s = b[r];\n    for (let k = r + 1; k < 8; k++) s -= A[r][k] * h[k];\n    h[r] = s / A[r][r];\n  }\n  return h;\n}\n\n// ---- glyph templates ----\n// One reference image per alphabet symbol, produced by replaying the real\n// chain (badge screen -> camera optics -> rectifying homography -> the\n// threshold above) so they look like what the scanner actually sees rather\n// than like crisp font renders, which match far worse. The badge's own font\n// was fitted against a real scan at em ~35 canvas px with a ~0.88 horizontal\n// squeeze -- taller and narrower than plain Montserrat 16, and getting that\n// wrong alone cost ~46 points of accuracy. 17 symbols, 30x40 px, 8-bit grey.\nconst GLYPH_TPL_B64 = \"\";\n\n// Template-matching decoder for the badge code screen.\n//\n// Tesseract reads this screen poorly: it is a blocky bitmap font at a size\n// its English model was never trained on, and thresholding a photo merges\n// neighbouring strokes.  But the badge only ever draws 17 possible symbols\n// in one known font at one known size, so each symbol can be matched\n// directly against a reference image of itself.\n//\n// Glyph advances run from 16 to 31 canvas px, so where each symbol sits\n// depends on the very text being read.  Each line is therefore decoded with\n// a beam search that walks left to right: every candidate is scored with a\n// small local x-jitter and the pen then advances by that candidate's own\n// width, so a wrong guess immediately misaligns everything after it and\n// loses to the right one.  Each comparison is confined to the candidate's\n// own advance width, so a neighbouring glyph cannot contaminate the score.\n\nconst GLYPH = (() => {\n  const KEYS = \"XAEFHKLMNPRTYJ479\";\n  const ADV = { X:20.724, A:22.544, E:20.634, F:19.551, H:25.01, K:22.153, L:18.303, M:29.416, N:25.01, P:22.243, R:22.393, T:18.077, Y:19.927, J:15.806, \"4\":20.604, \"7\":18.423, \"9\":19.009, \" \":8.287, G:23.777, \"1\":11.4 };\n  const CELL_H = 30, CELL_W = 40, PEN_PAD = 7, CAP_ROW = 3, MARGIN = 3;\n  const FW = 640, FH = 480;\n  const LINE_TOP_BADGE = 38, LINE_PITCH_BADGE = 21;\n  const CHARS_PER_LINE = [24,24,24,24,24,24,24,4];\n\n  let TPL = null;   // per-char {lo, w, vec:Float32Array} (centred + unit norm)\n\n  function b64bytes(b64) {\n    if (typeof atob === \"function\") {\n      const s = atob(b64), a = new Uint8Array(s.length);\n      for (let i = 0; i < s.length; i++) a[i] = s.charCodeAt(i);\n      return a;\n    }\n    return new Uint8Array(Buffer.from(b64, \"base64\"));\n  }\n\n  function init(b64) {\n    const raw = b64bytes(b64);\n    TPL = {};\n    const per = CELL_H * CELL_W;\n    for (let k = 0; k < KEYS.length; k++) {\n      const c = KEYS[k], off = k * per;\n      const lo = Math.max(0, PEN_PAD - MARGIN);\n      const hi = Math.min(CELL_W, PEN_PAD + Math.round(ADV[c]) + MARGIN);\n      const w = hi - lo;\n      const v = new Float32Array(CELL_H * w);\n      let sum = 0;\n      for (let r = 0; r < CELL_H; r++)\n        for (let j = 0; j < w; j++) {\n          const val = raw[off + r * CELL_W + lo + j];\n          v[r * w + j] = val; sum += val;\n        }\n      const mean = sum / v.length;\n      let nrm = 0;\n      for (let i = 0; i < v.length; i++) { v[i] -= mean; nrm += v[i] * v[i]; }\n      nrm = Math.sqrt(nrm) || 1e-6;\n      for (let i = 0; i < v.length; i++) v[i] /= nrm;\n      TPL[c] = { lo, w, vec: v };\n    }\n  }\n\n  // Match score for every glyph at every integer pen position on one line.\n  // Built once per line: the beam search revisits the same positions many\n  // times, and (because only advances are scaled) it is scale-independent.\n  function scoreTable(band, W) {\n    const K = KEYS.length;\n    const tab = new Float32Array((W + 1) * K).fill(-1);\n    for (let k = 0; k < K; k++) {\n      const t = TPL[KEYS[k]], w = t.w, vec = t.vec, n = CELL_H * w;\n      for (let x = 0; x <= W; x++) {\n        const c0 = x - PEN_PAD + t.lo;\n        if (c0 < 0 || c0 + w > W) continue;\n        let sum = 0, sq = 0, dot = 0;\n        for (let r = 0; r < CELL_H; r++) {\n          const rowOff = r * W + c0, vOff = r * w;\n          for (let j = 0; j < w; j++) {\n            const p = band[rowOff + j];\n            sum += p; sq += p * p; dot += p * vec[vOff + j];\n          }\n        }\n        const mean = sum / n;\n        // vec is already zero-mean, so sum(p*vec) == sum((p-mean)*vec)\n        const varr = sq - n * mean * mean;\n        tab[x * K + k] = varr > 1e-6 ? dot / Math.sqrt(varr) : 0;\n      }\n    }\n    return tab;\n  }\n\n  const JIT = [-2, -1, 0, 1, 2];\n\n  function bestAt(tab, W, x) {\n    const K = KEYS.length;\n    const sc = new Float32Array(K), dj = new Float32Array(K);\n    for (let k = 0; k < K; k++) { sc[k] = -2; dj[k] = 0; }\n    for (const d of JIT) {\n      let xi = Math.round(x + d);\n      if (xi < 0) xi = 0; if (xi > W) xi = W;\n      const base = xi * K;\n      for (let k = 0; k < K; k++) {\n        const v = tab[base + k];\n        if (v > sc[k]) { sc[k] = v; dj[k] = d; }\n      }\n    }\n    return { sc, dj };\n  }\n\n  function slotsFor(n) {\n    const out = [];\n    for (let g = 0; g * 4 < n; g++) {\n      for (let i = 0; i < Math.min(4, n - g * 4); i++) out.push(null);\n      if ((g + 1) * 4 < n) out.push(\" \");\n    }\n    return out;\n  }\n\n  function decodeLine(tab, W, n, advScale, startX, prefix, beam) {\n    let hyps = [{ s: 0, x: startX, t: \"\", pos: [] }];\n    let ci = 0;\n    for (const slot of slotsFor(n)) {\n      if (slot === \" \") {\n        for (const h of hyps) h.x += ADV[\" \"] * advScale;\n        continue;\n      }\n      const forced = (prefix && ci < prefix.length) ? prefix[ci] : null;\n      const nxt = [];\n      for (const h of hyps) {\n        const { sc, dj } = bestAt(tab, W, h.x);\n        if (forced !== null) {\n          const k = KEYS.indexOf(forced);\n          if (k < 0) {  // tag glyph we have no template for: just advance\n            nxt.push({ s: h.s, x: h.x + ADV[forced] * advScale, t: h.t + forced,\n                       pos: h.pos.concat([h.x]) });\n          } else {\n            nxt.push({ s: h.s + sc[k], x: h.x + dj[k] + ADV[forced] * advScale,\n                       t: h.t + forced, pos: h.pos.concat([h.x + dj[k]]) });\n          }\n        } else {\n          for (let k = 0; k < KEYS.length; k++) {\n            const c = KEYS[k];\n            nxt.push({ s: h.s + sc[k], x: h.x + dj[k] + ADV[c] * advScale,\n                       t: h.t + c, pos: h.pos.concat([h.x + dj[k]]) });\n          }\n        }\n      }\n      nxt.sort((a, b) => b.s - a.s);\n      hyps = nxt.slice(0, beam);\n      ci++;\n    }\n    return hyps[0];\n  }\n\n  // Locate a line's true cap-top near where the firmware says it should be.\n  function snapTop(gray, i) {\n    const nom = (LINE_TOP_BADGE + i * LINE_PITCH_BADGE) * 2;\n    let best = null, by = nom;\n    for (let dy = -8; dy <= 8; dy++) {\n      const y = nom + dy;\n      if (y < 0 || y + CELL_H > FH) continue;\n      let first = -1, tot = 0;\n      for (let r = 0; r < CELL_H; r++) {\n        let cnt = 0;\n        for (let x = 0; x < FW; x++) if (gray[(y + r) * FW + x] > 110) cnt++;\n        if (cnt > 3 && first < 0) first = r;\n        tot += cnt;\n      }\n      if (first < 0) continue;\n      const sc = -Math.abs(first - CAP_ROW) * 1000 + tot;\n      if (best === null || sc > best) { best = sc; by = y; }\n    }\n    return by;\n  }\n\n  function decodePayload(gray, opts) {\n    opts = opts || {};\n    const beam = opts.beam || 10;\n    const scales = opts.scales || [0.95, 1.0, 1.05];\n    let text = \"\";\n    const scores = [];\n    for (let i = 0; i < 8; i++) {\n      const n = CHARS_PER_LINE[i];\n      const y = snapTop(gray, i);\n      const band = new Float32Array(CELL_H * FW);\n      for (let r = 0; r < CELL_H; r++)\n        for (let x = 0; x < FW; x++) band[r * FW + x] = gray[(y + r) * FW + x];\n      let inkL = 40;\n      for (let x = 0; x < FW; x++) {\n        let any = false;\n        for (let r = 0; r < CELL_H && !any; r++) if (band[r * FW + x] > 110) any = true;\n        if (any) { inkL = x; break; }\n      }\n      const tab = scoreTable(band, FW);\n      const pre = i === 0 ? \"GP1\" : null;\n      let best = null;\n      for (const sc of scales) {\n        const Wx = ADV.X * n * sc + ADV[\" \"] * sc * Math.floor((n - 1) / 4);\n        const starts = [inkL - 2, inkL, inkL + 2, (FW - Wx) / 2];\n        for (const st of starts) {\n          const h1 = decodeLine(tab, FW, n, sc, st, pre, beam);\n          let Wd = ADV[\" \"] * sc * Math.floor((n - 1) / 4);\n          for (const ch of h1.t) Wd += (ADV[ch] || 23) * sc;\n          const h2 = decodeLine(tab, FW, n, sc, (FW - Wd) / 2, pre, beam);\n          for (const h of [h1, h2]) if (!best || h.s > best.s) best = h;\n        }\n      }\n      text += best.t;\n      for (const x of best.pos) {\n        const { sc } = bestAt(tab, FW, x);\n        scores.push(sc);\n      }\n    }\n    return { text, scores, keys: KEYS };\n  }\n\n  return { init, decodePayload, KEYS, ADV, CELL_H, CELL_W };\n})();\n\n// Checksum repair over the badge's row/column product code.\n//\n// Every data cell sits in exactly one row check and one column check, so an\n// isolated wrong cell shows up as one failing row AND one failing column and\n// is pinned to their intersection.  Repairs are chosen from the symbols the\n// matcher actually ranked highly, so a fix has to be visually plausible as\n// well as arithmetically consistent - otherwise the checksums can be\n// satisfied by a value the image never supported, producing a grid that\n// validates but is wrong.\n\nconst FIX = (() => {\n  const N = 12, MOD = 17;\n\n  function status(v) {\n    const badRows = [], badCols = [];\n    for (let y = 0; y < N; y++) {\n      let s = 0;\n      for (let x = 0; x < N; x++) s += v[y * 13 + x];\n      if (s % MOD !== v[y * 13 + 12]) badRows.push(y);\n    }\n    let tot = 0;\n    for (let x = 0; x < N; x++) {\n      let s = 0;\n      for (let y = 0; y < N; y++) s += v[y * 13 + x];\n      tot += s;\n      if (s % MOD !== v[156 + x]) badCols.push(x);\n    }\n    return { badRows, badCols, totalOk: tot % MOD === v[168] };\n  }\n\n  function nViol(v) {\n    const st = status(v);\n    return st.badRows.length + st.badCols.length + (st.totalOk ? 0 : 1);\n  }\n\n  function suspects(st) {\n    const s = new Set();\n    for (const y of st.badRows) {\n      s.add(y * 13 + 12);\n      for (let x = 0; x < N; x++) s.add(y * 13 + x);\n    }\n    for (const x of st.badCols) {\n      s.add(156 + x);\n      for (let y = 0; y < N; y++) s.add(y * 13 + x);\n    }\n    if (!st.totalOk) s.add(168);\n    return [...s];\n  }\n\n  // Arithmetic-only fallback: repair what the checksums uniquely determine.\n  function hardCorrect(v) {\n    v = v.slice();\n    for (let it = 0; it < 6; it++) {\n      const st = status(v);\n      if (!st.badRows.length && !st.badCols.length && st.totalOk) break;\n      let moved = false;\n      if (st.badRows.length && !st.badCols.length) {\n        for (const y of st.badRows) {\n          let s = 0;\n          for (let x = 0; x < N; x++) s += v[y * 13 + x];\n          v[y * 13 + 12] = s % MOD; moved = true;\n        }\n      } else if (st.badCols.length && !st.badRows.length) {\n        for (const x of st.badCols) {\n          let s = 0;\n          for (let y = 0; y < N; y++) s += v[y * 13 + x];\n          v[156 + x] = s % MOD; moved = true;\n        }\n      } else {\n        outer:\n        for (const y of st.badRows) {\n          for (const x of st.badCols) {\n            let rs = 0;\n            for (let i = 0; i < N; i++) if (i !== x) rs += v[y * 13 + i];\n            const want = ((v[y * 13 + 12] - rs) % MOD + MOD) % MOD;\n            let cs = 0;\n            for (let j = 0; j < N; j++) if (j !== y) cs += v[j * 13 + x];\n            if ((cs + want) % MOD === v[156 + x] && want !== v[y * 13 + x]) {\n              v[y * 13 + x] = want; moved = true; break outer;\n            }\n          }\n        }\n      }\n      const st2 = status(v);\n      if (!st2.badRows.length && !st2.badCols.length && !st2.totalOk) {\n        let t = 0;\n        for (let y = 0; y < N; y++) for (let x = 0; x < N; x++) t += v[y * 13 + x];\n        v[168] = t % MOD; moved = true;\n      }\n      if (!moved) break;\n    }\n    return v;\n  }\n\n  /** scores: array of Float32Array(17) per payload symbol; keys: symbol order. */\n  function correct(v, scores, keys, opts) {\n    opts = opts || {};\n    const topk = opts.topk || 4, beam = opts.beam || 6;\n    const steps = opts.steps || 6, maxPen = opts.maxPenalty || 0.55;\n    if (!scores || !scores.length) return { v: hardCorrect(v), ok: null };\n\n    // rank each position's alternatives by how much match score they give up.\n    // keys is the alphabet in value order, so an index here IS the cell value.\n    const ranked = scores.map((s) => {\n      const order = [];\n      for (let k = 0; k < s.length; k++) order.push(k);\n      order.sort((a, b) => s[b] - s[a]);\n      const best = s[order[0]];\n      return order.slice(0, topk).map((k) => ({ val: k, cost: best - s[k] }));\n    });\n\n    let states = [{ pen: 0, v: v.slice() }];\n    const seen = new Set();\n    for (let step = 0; step < steps; step++) {\n      for (const st of states) if (nViol(st.v) === 0) return { v: st.v, ok: true };\n      const nxt = [];\n      for (const stt of states) {\n        const st = status(stt.v);\n        for (const p of suspects(st)) {\n          const alts = ranked[p] || [];\n          for (const a of alts) {\n            if (a.val < 0 || a.val === stt.v[p]) continue;\n            const pen = stt.pen + a.cost;\n            if (pen > maxPen) continue;\n            const w = stt.v.slice();\n            w[p] = a.val;\n            const key = w.join(\",\");\n            if (seen.has(key)) continue;\n            seen.add(key);\n            nxt.push({ n: nViol(w), pen, v: w });\n          }\n        }\n      }\n      if (!nxt.length) break;\n      nxt.sort((a, b) => (a.n - b.n) || (a.pen - b.pen));\n      states = nxt.slice(0, beam).map((t) => ({ pen: t.pen, v: t.v }));\n      for (const s of states) if (nViol(s.v) === 0) return { v: s.v, ok: true };\n    }\n    const hv = hardCorrect(v);\n    return { v: hv, ok: nViol(hv) === 0 };\n  }\n\n  return { status, correct, hardCorrect, nViol };\n})();\n\nGLYPH.init(GLYPH_TPL_B64);\n\n// The rectified screen, kept in two forms. The canvas shown on screen stays\n// hard-thresholded because that's what reads clearly as \"this is what the\n// scanner saw\", but the glyph matcher gets the underlying GREYSCALE: the\n// threshold is what welds neighbouring strokes into one blob, and throwing\n// that detail away before matching costs real accuracy.\nlet lastFlatGray = null;\n\nfunction flatten(snapCanvas) {\n  const sctx = snapCanvas.getContext(\"2d\");\n  const img = sctx.getImageData(0, 0, snapCanvas.width, snapCanvas.height);\n  const blobs = findCornerBlobs(img);\n  const corners = classifyCorners(blobs);\n  if (!corners) { lastFlatGray = null; return null; }\n  const { tl, tr, bl, br } = corners;\n  const sx = FW / 320, sy = FH / 240;\n  const dst = [[10 * sx, 10 * sy], [310 * sx, 10 * sy], [10 * sx, 230 * sy], [310 * sx, 230 * sy]];\n  const h = homography(dst, [tl, tr, bl, br]);\n  const out = $(\"flat\");\n  out.width = FW; out.height = FH;\n  const octx = out.getContext(\"2d\");\n  const oimg = octx.createImageData(FW, FH);\n  const gray = new Float32Array(FW * FH);\n  const sw = snapCanvas.width, shh = snapCanvas.height, sd = img.data;\n  for (let y = 0; y < FH; y++)\n    for (let x = 0; x < FW; x++) {\n      const w = h[6] * x + h[7] * y + 1;\n      const u = (h[0] * x + h[1] * y + h[2]) / w;\n      const v = (h[3] * x + h[4] * y + h[5]) / w;\n      const o = (y * FW + x) * 4;\n      const ui = Math.round(u), vi = Math.round(v);\n      if (ui >= 0 && ui < sw && vi >= 0 && vi < shh) {\n        const i = (vi * sw + ui) * 4;\n        const lum = (sd[i] + sd[i + 1] + sd[i + 2]) / 3;\n        gray[y * FW + x] = lum;\n        const g = lum > 110 ? 255 : 0;\n        oimg.data[o] = oimg.data[o + 1] = oimg.data[o + 2] = g;\n      }\n      oimg.data[o + 3] = 255;\n    }\n  octx.putImageData(oimg, 0, 0);\n  out.style.display = \"block\";\n  lastFlatGray = gray;\n  return out;\n}\n\n// ---- camera + OCR ----\n//\n// Single button, three states, so the person never sees two separate\n// \"start\" and \"scan\" controls competing for attention:\n//   idle    -> \"Start Camera\" (default on page load / after nothing's\n//              been captured yet)\n//   live    -> a red \"Capture\" button, shown once the camera stream is\n//              actually playing\n//   review  -> \"Retake\", shown once a photo's been captured; the video\n//              feed is swapped out for the captured still frame so it's\n//              clear what was actually scanned\n// Capturing immediately runs flatten() + OCR + decode() with no separate\n// confirmation step — that's what \"scanned immediately without user\n// intervention\" means here, matching what snapBtn's old handler already\n// did on click; the only real change is that click now happens as part\n// of the single capture button rather than a second dedicated button.\nlet stream = null;\nlet camState = \"idle\";\n\nfunction stopCameraStream() {\n  if (stream) {\n    stream.getTracks().forEach((t) => t.stop());\n    stream = null;\n  }\n}\n\nfunction setCamState(next, opts) {\n  opts = opts || {};\n  camState = next;\n  const icon = $(\"camBtnIcon\");\n  const label = $(\"camBtnLabel\");\n  const btn = $(\"camBtn\");\n  const v = $(\"video\");\n  const snapCanvas = $(\"snap\");\n  const flatCanvas = $(\"flat\");\n  const ph = $(\"camPlaceholder\");\n  const noRect = $(\"noRectifiedPlaceholder\");\n\n  if (next === \"idle\") {\n    icon.textContent = \"videocam\";\n    icon.className = \"material-symbols-outlined text-sm text-purple-400\";\n    label.textContent = \"Start Camera\";\n    btn.className = \"w-full flex items-center justify-center gap-2 py-2.5 px-3 rounded-xl bg-slate-800/90 hover:bg-slate-700/80 active:scale-[0.98] border border-white/10 text-slate-200 text-xs font-semibold transition-all shadow-sm\";\n    // Initial launch: only the placeholder is visible, nothing else.\n    v.style.display = \"none\";\n    snapCanvas.style.display = \"none\";\n    flatCanvas.style.display = \"none\";\n    if (noRect) noRect.style.display = \"none\";\n    if (ph) ph.style.display = \"flex\";\n  } else if (next === \"live\") {\n    icon.textContent = \"fiber_manual_record\";\n    icon.className = \"material-symbols-outlined text-sm text-white\";\n    label.textContent = \"Capture\";\n    btn.className = \"w-full flex items-center justify-center gap-2 py-2.5 px-3 rounded-xl bg-rose-600 hover:bg-rose-500 active:scale-[0.98] text-white text-xs font-semibold shadow-md shadow-rose-600/30 transition-all\";\n    // Capture mode: only the live camera feed is visible. Any leftover\n    // captured/rectified image from a previous scan is cleared so a\n    // retake never briefly shows stale content.\n    v.style.display = \"block\";\n    snapCanvas.style.display = \"none\";\n    flatCanvas.style.display = \"none\";\n    if (noRect) noRect.style.display = \"none\";\n    if (ph) ph.style.display = \"none\";\n  } else if (next === \"review\") {\n    icon.textContent = \"replay\";\n    icon.className = \"material-symbols-outlined text-sm text-slate-200\";\n    label.textContent = \"Retake\";\n    btn.className = \"w-full flex items-center justify-center gap-2 py-2.5 px-3 rounded-xl bg-slate-800/90 hover:bg-slate-700/80 active:scale-[0.98] border border-white/10 text-slate-200 text-xs font-semibold transition-all shadow-sm\";\n    // After capture: show only the rectified/OCR'd image (flat canvas),\n    // never the raw captured photo (snap) -- the person should see what\n    // the scanner actually read, not just what the camera saw. When\n    // corner detection failed there's no rectified image to show; rather\n    // than fall back to the raw photo, show an explanatory placeholder\n    // instead (opts.rectified === false signals this case).\n    v.style.display = \"none\";\n    snapCanvas.style.display = \"none\";\n    if (opts.rectified === false) {\n      flatCanvas.style.display = \"none\";\n      if (noRect) noRect.style.display = \"flex\";\n    } else {\n      flatCanvas.style.display = \"block\";\n      if (noRect) noRect.style.display = \"none\";\n    }\n    if (ph) ph.style.display = \"none\";\n  }\n}\n\nasync function startCamera() {\n  try {\n    stream = await navigator.mediaDevices.getUserMedia({ video: { facingMode: \"environment\" } });\n    const v = $(\"video\");\n    v.srcObject = stream;\n    await v.play();\n    setCamState(\"live\");\n  } catch (e) {\n    $(\"status\").textContent = \"Camera unavailable (needs HTTPS or localhost): \" + e.message +\n      \"\\nUse the text box instead.\";\n  }\n}\n\n// The live preview shows the camera stream through CSS `object-fit: cover`\n// at a fixed 16:9 box (see the video/canvas.snap rule above), which\n// center-crops the native stream to that ratio -- exactly like a photo app\n// viewfinder. That crop is NOT cosmetic: it's the only part of the frame\n// the person actually sees and aims the 4 corner squares within. Capturing\n// the full, uncropped v.videoWidth x v.videoHeight frame (the old\n// behavior) grabbed extra field-of-view the person never saw and had no\n// reason to keep clear -- stray bright objects out there (lights,\n// reflections, glare) could dominate corner-blob detection even when the\n// badge itself was framed perfectly in the visible preview. Replicating\n// object-fit: cover's own center-crop math here means what gets scanned\n// is exactly what was shown on screen.\nfunction coverCropRect(sourceW, sourceH, targetAspect) {\n  const sourceAspect = sourceW / sourceH;\n  if (sourceAspect > targetAspect) {\n    // Source is relatively wider than the target box -- crop left/right.\n    const cropW = sourceH * targetAspect;\n    return { sx: (sourceW - cropW) / 2, sy: 0, sw: cropW, sh: sourceH };\n  } else {\n    // Source is relatively taller than the target box -- crop top/bottom.\n    const cropH = sourceW / targetAspect;\n    return { sx: 0, sy: (sourceH - cropH) / 2, sw: sourceW, sh: cropH };\n  }\n}\n\nasync function captureAndScan() {\n  const v = $(\"video\"), c = $(\"snap\");\n  const crop = coverCropRect(v.videoWidth, v.videoHeight, 16 / 9);\n  c.width = Math.round(crop.sw);\n  c.height = Math.round(crop.sh);\n  c.getContext(\"2d\").drawImage(\n    v,\n    crop.sx, crop.sy, crop.sw, crop.sh,\n    0, 0, c.width, c.height\n  );\n\n  // Stop the live feed immediately (before OCR runs) so there's no dead\n  // time where the button says \"Retake\" but the video feed is still live\n  // and moving. flatten() runs synchronously right after, so camState can\n  // move straight to \"review\" already knowing whether a rectified image\n  // exists -- the raw photo (snap) is never shown, only the rectified\n  // result or, failing that, an explanatory placeholder (see setCamState).\n  stopCameraStream();\n\n  // Clear the raw code box so the incoming OCR result is what's visibly\n  // there, not a stale previous decode left over from an earlier scan.\n  $(\"code\").value = \"\";\n\n  const flat = flatten(c);\n  setCamState(\"review\", { rectified: !!flat });\n  $(\"status\").textContent = flat\n    ? \"Corners found. Reading text...\"\n    : \"Could not find all 4 corner squares, reading the raw photo instead...\";\n  try {\n    if (flat && lastFlatGray) {\n      // Once the screen is rectified, its geometry is fully known from the\n      // firmware (8 lines, 21px pitch, one of 17 possible symbols per cell),\n      // so the glyph matcher reads it directly instead of asking Tesseract\n      // to recognise a bitmap font it was never trained on. Tesseract stays\n      // on as the fallback below for when the corners can't be found and\n      // there's no reliable geometry to match against.\n      const r = GLYPH.decodePayload(lastFlatGray);\n      $(\"code\").value = groupCode(r.text);\n      // scores line up with r.text; the payload starts after the \"GP1\" tag\n      decode(r.text, { glyphScores: r.scores.slice(3) });\n      // Show what was actually decoded, including any checksum repair, so\n      // the box and the grid below it never disagree.\n      if (lastDecodedPayload.length === 169) {\n        $(\"code\").value = groupCode(\"GP1\" + lastDecodedPayload);\n      }\n    } else {\n      const text = await ocrWholeImage(c);\n      // Tesseract's raw output carries stray characters and its own line\n      // breaks; strip it to the alphabet decode() actually reads so the\n      // grouping shown matches the characters being decoded.\n      $(\"code\").value = groupCode(text.toUpperCase().replace(/[^XAEFHKLMNPRTYJ479]/g, \"\"));\n      decode(text, { autoCorrect: true });\n    }\n  } catch (e) {\n    $(\"status\").textContent = \"Scan failed: \" + e.message;\n  }\n}\n\n// Lay the code out the way the badge itself does: groups of four, six groups\n// to a line. The glyph decoder always returns exactly 172 symbols, so the\n// grouping lines up with the badge screen one-for-one (7 full lines plus a\n// final \"FFX4\"), which makes checking a scan against the screen a matter of\n// reading across. Callers pass an already-clean string; decode() strips\n// whitespace anyway, so this is purely for reading and stays hand-editable.\nfunction groupCode(s) {\n  const groups = [];\n  for (let i = 0; i < s.length; i += 4) groups.push(s.slice(i, i + 4));\n  const lines = [];\n  for (let i = 0; i < groups.length; i += 6) lines.push(groups.slice(i, i + 6).join(\" \"));\n  return lines.join(\"\\n\");\n}\n\n// ---- OCR: whole-image (fallback) vs line-by-line (preferred) ----\n\nasync function ocrWholeImage(source) {\n  const worker = await Tesseract.createWorker(\"eng\");\n  await worker.setParameters({\n    tessedit_char_whitelist: \"XAEFHKLMNPRTYJ479 \",\n    tessedit_pageseg_mode: \"6\",\n  });\n  const { data } = await worker.recognize(source);\n  await worker.terminate();\n  return data.text;\n}\n\n// Per-line Tesseract OCR used to live here. It was replaced by the glyph\n// matcher above: with the screen rectified, the firmware pins every symbol's\n// position and the alphabet is only 17 characters, so matching each cell\n// against a reference image of itself beats asking a general-purpose OCR\n// engine to recognise a bitmap font it has never seen. Whole-image\n// Tesseract (ocrWholeImage) is still the fallback when the corner squares\n// can't be found and there is no geometry to match against.\n\nfunction retakePhoto() {\n  // startCamera() moves camState to \"live\" on success, which itself hides\n  // the captured/rectified canvases and any review placeholder (see\n  // setCamState) -- so retaking cleanly removes any taken image and shows\n  // only the live camera feed, with nothing left over from the last scan.\n  startCamera();\n}\n\n$(\"camBtn\").onclick = () => {\n  if (camState === \"idle\") {\n    startCamera();\n  } else if (camState === \"live\") {\n    captureAndScan();\n  } else if (camState === \"review\") {\n    retakePhoto();\n  }\n};\n\n// ---- Solana Submit Interaction (real gas-station call) ----\nlet currentTxSignature = \"\";\n\n$(\"solanaSubmitBtn\").onclick = async () => {\n  if (!lastDecodeOk) {\n    $(\"status\").textContent = \"Fix the checksum errors above before submitting.\";\n    return;\n  }\n  const btn = $(\"solanaSubmitBtn\");\n  const label = $(\"submitBtnLabel\");\n\n  btn.disabled = true;\n  label.innerHTML = \"Broadcasting to Solana...\";\n  $(\"status\").textContent = \"Sending payload to the gas station...\\nBuilding compressed-tree append transaction...\";\n\n  try {\n    const base = apiBase();\n    if (!base) throw new Error(\"Gas station URL is not configured.\");\n    const res = await fetch(base.replace(/\\/$/, \"\") + \"/api/submit\", {\n      method: \"POST\",\n      headers: { \"Content-Type\": \"application/json\" },\n      body: JSON.stringify({ code: lastDecodedPayload, pixels: pix }),\n    });\n    const body = await res.json().catch(() => ({}));\n    if (!res.ok || !body.ok) {\n      throw new Error(body.error || (\"Gas station returned HTTP \" + res.status));\n    }\n\n    currentTxSignature = body.signature;\n    const card = $(\"solanaTxCard\");\n    const sigElem = $(\"txSig\");\n    const slotElem = $(\"txSlot\");\n\n    if (card && sigElem) {\n      sigElem.textContent = currentTxSignature.slice(0, 8) + \"...\" + currentTxSignature.slice(-8);\n      sigElem.title = currentTxSignature;\n      if (slotElem) slotElem.textContent = body.slot ? (\"Slot #\" + body.slot) : \"Slot #—\";\n      card.classList.remove(\"hidden\");\n      card.classList.add(\"flex\");\n    }\n\n    label.innerHTML = \"Submitted to Solana!\";\n    btn.classList.add(\"ring-2\", \"ring-teal-400\");\n    $(\"status\").textContent = \"Transaction confirmed on Solana \" + (body.cluster || \"devnet\") + \"!\\n\" +\n      \"Signature: \" + currentTxSignature +\n      \"\\nLeaf hash: \" + (body.leafHash ?? \"—\") +\n      \"\\nPayload: 12x12 badge matrix appended to the compression tree.\";\n\n    // The feed doesn't know about this submission until the next poll\n    // (or tab switch) re-fetches /api/feed — invalidate the cached list\n    // so switching to Drawings shows it immediately rather than a stale\n    // page from before this submission landed.\n    feedNeedsRefresh = true;\n  } catch (e) {\n    $(\"status\").textContent = \"Submission failed: \" + e.message;\n    label.innerHTML = \"Publish New Image\";\n    btn.disabled = false;\n    return;\n  }\n\n  setTimeout(() => {\n    label.innerHTML = \"Publish New Image\";\n    btn.disabled = !lastDecodeOk;\n    btn.classList.remove(\"ring-2\", \"ring-teal-400\");\n  }, 2800);\n};\n\nfunction copyTxSignature() {\n  if (currentTxSignature) {\n    navigator.clipboard.writeText(currentTxSignature).then(() => {\n      const orig = $(\"status\").textContent;\n      $(\"status\").textContent = \"Copied TX signature to clipboard: \" + currentTxSignature;\n      setTimeout(() => { $(\"status\").textContent = orig; }, 2500);\n    });\n  }\n}\n\n// UI demo seed\nfunction sampleDemoCode() {\n  const sample = \"GP1\" +\n    \"XXXXXXXXXXXXX\" +\n    \"XXXXXAAAAXXXX\" +\n    \"XXXXAAAAAAXXX\" +\n    \"XXXAAAAEAAAAX\" +\n    \"XXXAAEEEEAAAX\" +\n    \"XXXXEEEEEEAAX\" +\n    \"XXXXXEEEEAAAX\" +\n    \"XXXXXXEAAAAAX\" +\n    \"XXXXXXAAAAAXX\" +\n    \"XXXXXAAAAAAXX\" +\n    \"XXXXXXAAAAXXX\" +\n    \"XXXXXXXXXXXXX\" +\n    \"XXXXXXXXXXXXX\";\n  $(\"code\").value = sample;\n  decode(sample);\n}\n\n// ---- Tab switching ----\nfunction switchToTab(tab) {\n  const feedSection = $(\"feedSection\");\n  const uploadSection = $(\"uploadSection\");\n  const tabFeedBtn = $(\"tabFeedBtn\");\n  const tabUploadBtn = $(\"tabUploadBtn\");\n\n  if (tab === \"feed\") {\n    feedSection.classList.remove(\"hidden\");\n    feedSection.classList.add(\"flex\");\n    uploadSection.classList.add(\"hidden\");\n    uploadSection.classList.remove(\"flex\");\n    tabFeedBtn.classList.add(\"active\");\n    tabUploadBtn.classList.remove(\"active\");\n    if (feedNeedsRefresh || !feedLoadedOnce) loadFeed({ reset: true });\n  } else {\n    uploadSection.classList.remove(\"hidden\");\n    uploadSection.classList.add(\"flex\");\n    feedSection.classList.add(\"hidden\");\n    feedSection.classList.remove(\"flex\");\n    tabUploadBtn.classList.add(\"active\");\n    tabFeedBtn.classList.remove(\"active\");\n  }\n  window.scrollTo({ top: 0, behavior: \"smooth\" });\n}\n\n// ---- Doomscroll feed (real data from GET /api/feed) ----\nlet feedLoadedOnce = false;\nlet feedNeedsRefresh = false;\nlet feedNextBefore = null;\nlet feedPollTimer = null;\n// Every signature currently rendered in the DOM, so a periodic refresh\n// can tell \"brand new\" apart from \"already showing\" and only prepend\n// what's actually new — see pollForNewImages() below.\nlet feedSeenSignatures = new Set();\n\nfunction timeAgo(isoString) {\n  if (!isoString) return \"\";\n  const seconds = Math.max(0, Math.floor((Date.now() - new Date(isoString).getTime()) / 1000));\n  if (seconds < 60) return seconds + \"s ago\";\n  const minutes = Math.floor(seconds / 60);\n  if (minutes < 60) return minutes + \"m ago\";\n  const hours = Math.floor(minutes / 60);\n  if (hours < 24) return hours + \"h ago\";\n  return Math.floor(hours / 24) + \"d ago\";\n}\n\nfunction shortSig(sig) {\n  return sig.slice(0, 6) + \"...\" + sig.slice(-6);\n}\n\nfunction buildFeedCard(image) {\n  const article = document.createElement(\"article\");\n  article.className = \"bg-obsidian-900/90 border border-white/10 rounded-2xl p-4 shadow-xl relative overflow-hidden\";\n\n  const header = document.createElement(\"div\");\n  header.className = \"flex items-center justify-between mb-3 font-mono text-xs\";\n  header.innerHTML =\n    '<div class=\"flex items-center gap-1.5 text-slate-400\">' +\n      '<span class=\"text-slate-200 font-medium\">TX: ' + shortSig(image.signature) + '</span>' +\n      '<span class=\"text-slate-600\">•</span>' +\n      '<span class=\"text-slate-500 text-[11px]\">' + timeAgo(image.timestamp) + '</span>' +\n    '</div>' +\n    '<span class=\"text-[10px] px-2 py-0.5 rounded bg-slate-800/80 text-slate-300 border border-slate-700/50\">12×12 BADGE</span>';\n\n  const gridWrap = document.createElement(\"div\");\n  gridWrap.className = \"bg-obsidian-950/80 rounded-xl p-3 border border-white/5 mb-3 flex flex-col items-center\";\n\n  const grid = document.createElement(\"div\");\n  grid.className = \"w-full max-w-[260px] feed-grid\";\n  image.pixels.forEach((val) => {\n    const cell = document.createElement(\"div\");\n    cell.className = \"feed-cell\";\n    cell.style.backgroundColor = hex(COLORS[val] ?? 0xffffff);\n    grid.appendChild(cell);\n  });\n  gridWrap.appendChild(grid);\n\n  const footer = document.createElement(\"div\");\n  footer.className = \"w-full flex items-center justify-between mt-2.5 pt-2 border-t border-white/5 text-[10px] font-mono text-slate-500\";\n  footer.innerHTML =\n    '<span>SLOT #' + image.slot + '</span>' +\n    '<span class=\"text-emerald-400\">144 PIXELS STORED</span>';\n  gridWrap.appendChild(footer);\n\n  const actions = document.createElement(\"div\");\n  actions.className = \"flex items-center justify-between text-slate-400 pt-0.5\";\n  actions.innerHTML =\n    '<span class=\"text-[10px] font-mono text-slate-600\">On-chain • Solana</span>' +\n    '<button class=\"text-[11px] font-mono text-slate-500 hover:text-slate-300 flex items-center gap-1\">' +\n      '<span class=\"material-symbols-outlined text-[14px]\">content_copy</span>' +\n      '<span>Copy TX</span>' +\n    '</button>';\n  actions.querySelector(\"button\").onclick = () => {\n    navigator.clipboard.writeText(image.signature).catch(() => {});\n  };\n\n  article.appendChild(header);\n  article.appendChild(gridWrap);\n  article.appendChild(actions);\n  return article;\n}\n\nasync function loadFeed(opts) {\n  opts = opts || {};\n  const reset = !!opts.reset;\n  const base = apiBase();\n  const statusEl = $(\"feedStatus\");\n  const listEl = $(\"feedList\");\n  const loadMoreBtn = $(\"feedLoadMoreBtn\");\n\n  if (!base) {\n    statusEl.innerHTML = '<span class=\"text-amber-400\">Gas station URL is not configured — cannot load the feed.</span>';\n    return;\n  }\n\n  if (reset) {\n    feedNextBefore = null;\n    feedSeenSignatures = new Set();\n    listEl.innerHTML = \"\";\n    statusEl.classList.remove(\"hidden\");\n    statusEl.innerHTML =\n      '<div class=\"w-5 h-5 border-2 border-emerald-400 border-t-transparent rounded-full animate-spin mb-2\"></div>' +\n      '<span>Syncing on-chain badge blocks...</span>';\n    loadMoreBtn.classList.add(\"hidden\");\n  }\n\n  try {\n    const url = new URL(base.replace(/\\/$/, \"\") + \"/api/feed\");\n    url.searchParams.set(\"limit\", \"20\");\n    if (feedNextBefore) url.searchParams.set(\"before\", feedNextBefore);\n\n    const res = await fetch(url.toString());\n    const body = await res.json();\n    if (!res.ok || !body.ok) throw new Error(body.error || (\"Feed returned HTTP \" + res.status));\n\n    feedLoadedOnce = true;\n    feedNeedsRefresh = false;\n    feedNextBefore = body.nextBefore || null;\n\n    if (reset && body.images.length === 0) {\n      statusEl.innerHTML = '<span>No drawings published yet. Be the first — scan a badge in Upload / Scan.</span>';\n    } else {\n      statusEl.classList.add(\"hidden\");\n    }\n\n    body.images.forEach((image) => {\n      feedSeenSignatures.add(image.signature);\n      listEl.appendChild(buildFeedCard(image));\n    });\n    loadMoreBtn.classList.toggle(\"hidden\", !feedNextBefore);\n  } catch (e) {\n    statusEl.classList.remove(\"hidden\");\n    statusEl.innerHTML = '<span class=\"text-rose-400\">Could not load the feed: ' + e.message + '</span>';\n  }\n}\n\n$(\"feedLoadMoreBtn\").onclick = () => loadFeed({ reset: false });\n\n// Periodic refresh so newly published drawings show up without a manual\n// pull-to-refresh. Deliberately NOT a reset+reload: that would wipe out\n// any \"Load more\" pages already fetched and reset scroll position every\n// time it fired, which is exactly the janky behavior a doomscroll feed\n// must not have. Instead this fetches just the newest page, compares\n// against what's already rendered (feedSeenSignatures), and prepends\n// only the genuinely new cards — everything already on screen, and any\n// older pages loaded via \"Load more\", are left untouched.\nasync function pollForNewImages() {\n  if ($(\"feedSection\").classList.contains(\"hidden\")) return; // only poll the visible tab\n  const base = apiBase();\n  if (!base) return;\n\n  try {\n    const url = new URL(base.replace(/\\/$/, \"\") + \"/api/feed\");\n    url.searchParams.set(\"limit\", \"20\");\n    const res = await fetch(url.toString());\n    const body = await res.json();\n    if (!res.ok || !body.ok) return; // silent on poll failures; the next tick retries\n\n    const freshImages = body.images.filter((image) => !feedSeenSignatures.has(image.signature));\n    if (!freshImages.length) return;\n\n    const listEl = $(\"feedList\");\n    const statusEl = $(\"feedStatus\");\n    statusEl.classList.add(\"hidden\"); // covers the empty-feed message once the first image arrives\n\n    // body.images arrives newest-first; insert in that same order at the\n    // very top so the newest submission ends up first in the list.\n    freshImages.forEach((image) => feedSeenSignatures.add(image.signature));\n    for (let i = freshImages.length - 1; i >= 0; i--) {\n      listEl.insertBefore(buildFeedCard(freshImages[i]), listEl.firstChild);\n    }\n  } catch {\n    // Network hiccup on a background poll — not worth surfacing to the\n    // user; loadFeed()'s own error handling covers the explicit-load path.\n  }\n}\n\nfunction startFeedPolling() {\n  if (feedPollTimer) clearInterval(feedPollTimer);\n  feedPollTimer = setInterval(pollForNewImages, 20000);\n}\n\n// Initial render of default empty matrix + first feed load\nrender();\nswitchToTab(\"feed\");\nstartFeedPolling();\n</script>\n\n\n</body></html>\n";
